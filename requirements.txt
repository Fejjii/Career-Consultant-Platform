# Streamlit Community Cloud installs from this file.
# Keep this list UI-only so deployment does not pull backend/eval dependencies.
streamlit>=1.39.0,<2.0.0
httpx>=0.27.0,<1.0.0
tiktoken>=0.8.0,<1.0.0
streamlit-audiorec>=0.1.3
